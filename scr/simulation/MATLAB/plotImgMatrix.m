function h = plotImgMatrix(mat)
%PLOTIMG Summary of this function goes here
%   Detailed explanation goes here

horizontal = size(mat,1);
vertical = size(mat,2);
for h=1:horizontal
   for v=1:vertical
      if mat(h,v)
         
end

for